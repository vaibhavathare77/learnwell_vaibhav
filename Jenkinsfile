pipeline {
    agent {}
    stages {
        stage('Date') {
            steps {
                sh 'date'
            }
        stage('Echo') {
            steps {
                sh 'echo "Hello World"'
            }
        }
    }
}
